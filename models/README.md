# Models

Place trained model files here after running the ML training pipeline:

- `cogniflow_rf_model.pkl` — Random Forest classifier (n=100, d=10, m=2)
- `le_state.pkl` — LabelEncoder for keyboard states
- `le_activity.pkl` — LabelEncoder for window activity labels

Train using: `python train_model.py`

